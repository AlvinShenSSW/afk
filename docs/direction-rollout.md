# Direction rollout, owner decision and rollback

The owner decision is PENDING. Actual dependency evidence and current #113 ordered review outcomes are attributed below. This document does not enable direction audits, change an existing run, authorize a model call or select a rollout option.

Direction remains an opt-in addition to the existing workflow. The template defaults to `off` with a four-attempt direction limit. Retaining that default is compatible with a pending, invalid or unavailable transport qualification record; a successful qualification is not required to decide against promotion. Required behavior and any unachieved benefit must remain visible.

The canonical operational contracts are [direction state](../skills/afk/references/direction-state.md), [direction audits](../skills/afk/references/direction-audit.md), [environment and shared-run location](../skills/afk/references/environment.md), and [review convergence](../skills/afk/references/review-convergence.md). This guide does not define another state schema or allowance.

## Evidence for the decision

The actual [#113 report](evaluations/issue-113-pilot.md) records source-capability **NO-GO**: the current observation adapter cannot qualify the required author-host surface. The exact report revision passed ordered Kimi then DeepSeek Flash publication reviews. No owner option has been selected.

| Evidence | Actual attribution | Limit |
| --- | --- | --- |
| Paired baseline B | `6244d63a87826e2834652fc8a96df6647933dce2` | Definition of paired comparison, not observed behavior. |
| Reviewed #112 implementation H | `c4b554bf7edb5a9ed0097a7f8de3aef628757fa0` | Structural reviews and 1,280 native tests: 1,273 passed, seven skipped, zero failed; separate affected integration 96/96. |
| #113 investigation C | `9fd041995828519200e033fb81cfa9b33563ae57` | H-to-C adds only its frozen design; actual report owns the 96-entry source comparison. |
| Current #113 report R | `4e100c38d3c3f55d46429a814e7b65da373fde88` | C-to-R adds only `docs/evaluations/issue-113-pilot.md`; ordered publication reviews accepted this exact revision. |
| #113 report digest | `223d3abd308bf3758ad94ddbb86ad2e7e9eb9f4360c61736437484801aee180b` | Source-capability NO-GO, not a failed host invocation. |
| #111 publication | `78ba796f307eb66b4431e448460c53dd57e95567` | Actual bounded transport/protocol qualification; no author-host or semantic qualification. |
| Qualified profile | `abb20b243641972c135eff402461ed8ff1a85ad332aa5597ba6ffe0e44239cb4` | Revalidate actual record/profile when adopting another revision. |
| Qualified record digest | `d502dbc47c9dbba54780b9d95044ea2df370a60e8eb8a09c621d535cd467cc37` | Actual status is qualified; no new call was made for #113. |
| Owner decision | PENDING option, actual source/date, scope, limits and residual disposition | Neither NO-GO nor unchanged off selects the owner's decision. |

The actual #113 records retain **0/72 main observations, 0/180 controls and all four unattempted prerequisites**. Its handoff is planned, has no selected model/cell/slot and no calling allocation. No host, prerequisite, auditor, control or relay call was launched. Preserve the distinct planned maximum of 92 main author stages and 180 control stages; none is measured execution. Source/Git export and empty aggregation are noncalling artifact operations. The report binds complete private records without publishing their local paths. These records do not authenticate every action outside the retained driver.

With no samples, semantic success/failure rates, observed model identity, token/cache use, billing and model-active time are unavailable. An empty aggregate's arithmetic zeros do not measure free execution. The [#111 qualification report](evaluations/issue-111-direction-audits.md) retains four driver attempts, three HTTP requests and 7,463 charged ms, including the invalid response. It records known-call retained-rate estimates of USD 0.0027609 and no provider bill; these are not #113 costs. #98's separate unmet `0/14` keeps its original evidence and accounting owner. Native-suite probes and structural reviewers also retain separate activity/accounting; their unknown usage does not become zero.

Critical Epic behavior remains OUTSTANDING without actual evidence: AC1 instruction loading/selection, AC2 authorized completion, AC4 direction detection/justified changes and AC6 faithful continuity. AC3/AC5 structural intent/accounting evidence supplies no new behavioral credit. AC7 requires an honest report; AC8 requires an explicit compatible rollout decision. A NO-GO report, a unit-test pass or a narrow transport qualification does not complete these missing outcomes.

The current R passed Kimi outer review and DeepSeek Flash final review, both APPROVE WITH COMMENTS on the same revision, base and configuration. Kimi K3 was selected locally; its provider response identity was unavailable. Flash returned the observed `deepseek-flash` alias and a valid stop. Its first reasoning-only response exhausted the output limit; one supported adjusted retry kept the full context and model with thinking disabled. Those review attempts remain separately accounted. They are not behavioral samples. The publication record owns final native-suite and revision-specific CI outcomes; review approval alone supplies neither empirical acceptance nor merge authority.

## Owner options

| Option | Decision needed | Evidence limit |
| --- | --- | --- |
| Retain off | Keep the current default and record the unachieved benefit and continued/deferred work | Existing initialized runs keep their retained policy until separately amended |
| Continue a bounded opt-in investigation | Name the eligible host/model/profile, selected cells, authority, attempt/time/spend limits and evidence owner in a concrete reviewed handoff | Unsupported whole-tool or catalog observation cannot be overcome by flags, copied mock observations or another identical attempt |
| Promote an evaluated default | Explicitly select the evaluated scope/default and authorize the corresponding reviewed release change | Ineligible while critical behavior is unavailable, required transport is unqualified, or unsafe completion, scope mutation or budget reset remains unexplained |

Promotion is currently ineligible because critical behavioral evidence is unavailable. A bounded investigation also needs a supported observation source and a new concrete authorized handoff; this document is not that handoff.

There is no automatic transition from a clean structural review to opt-in execution or default promotion. Every new investigation accounts for old consumed attempts and any unresolved evidence under their existing owners. Its launch authorization must be explicit and bounded.

## Current coverage and limitations

The source-capability assessment is specific to the current observation adapter. It supplies no complete supported tool/instruction inventory or observable native catalog; shell/network restrictions or copied mock rows cannot supply those missing observations. No author host/model pair was qualified by this campaign; Astra/Sol remain candidate labels, not observed responses. The qualified zero-tool auditor does not confine the author host. A repeat of the same unsupported prerequisite is not an evidence-based remedy.

The #113 report retains deferred boundaries: complete D9 history can exceed 16 KiB and refuse before charge; late reservation may consume a slot before dispatch refusal; some reference-read ordering remains unobserved; cleanup summaries can disagree with retained raw evidence; conflicting model labels can be masked by repetition. Future eligible evaluation must adjudicate relevant limits, not erase them because this report is NO-GO. The #111 report also retains fail-closed source-pattern, derived-path and result-read-size limitations. No such boundary is repaired by this guide.

## Mode semantics

- `off`: adds no direction audit call or direction-specific completion condition. Ordinary findings, tests, reviews, CI, publication limits and owner merge authority still apply.
- `shadow`: uses the same source, independence and call bounds while adding no direction-only hold or readiness condition. It does not bypass dispatch qualification or authorize extra calls.
- `required`: calls for an independent initial audit before implementation and a valid, current phase-`endpoint` `COMPLETE` for the selected endpoint and actual target before dependent completion. Initial/signal `COMPLETE`, `ON-TRACK`, stale or missing evidence cannot substitute. A previously charged last slot can still produce its valid result; exhaustion grants no next attempt.

A measurement-fixture `COMPLETE` is not approval of the original author's target or proof of semantic compliance. Source/hash checks constrain artifacts only when invoked; semantic judgment is level 1, helper-local artifact checks level 2, and truthful driver/owner execution level 3. AFK supplies no external reference monitor or guarantee of task alignment.

## Inspect before enabling or amending

Work from the consuming repository, resolve the installed plugin root through the canonical environment rules, and use the supplied run identity in the main working tree's ignored `.afk/runs/`. Do not allocate a new run merely to read a mode or avoid an exhausted allowance.

The following are command templates, not executed evidence. Replace each placeholder with an actual retained identity/path. Reads do not dispatch models:

```text
node "<plugin-root>/scripts/direction-state.mjs" check --run-id <run-id> --issue <issue-id>
node "<plugin-root>/scripts/direction-state.mjs" check --run-id <run-id> --issue <issue-id> --expected <expected.json>
node "<plugin-root>/scripts/check-direction-audit.mjs" check --run-id <run-id> --issue <issue-id> --audit <audit-id> --stage result
node "<plugin-root>/scripts/check-direction-audit.mjs" check --run-id <run-id> --issue <issue-id> --audit <audit-id> --stage endpoint --endpoint <endpoint-id>
```

`--expected` is optional and uses the schema's retained head/baseline/policy binding for an initialized state, not an unmodified whole CLI response. Its canonical JSON file must be inside the named run. Read status, reasons, mode, active digests, accounting and `canReserve`; exit zero alone is not permission to launch or complete work. An unavailable standalone audit check may report missing files/directories instead of literal `off`. Missing or corrupt state cannot discard a retained required policy.

Absent direction records remain off even when config changes. Explicit initialization is limited to an active run with source authorization, recoverable baseline and conservative prior direction accounting. Completed runs are history and are not reopened or migrated. An unknown prior consumption value cannot be replaced by zero or made known by raising a limit.

For an initialized policy, editing `.afk/config.md` does not amend it. Retain the operator's actual amendment source in the run, inspect the current head and construct the schema's canonical policy-successor request. The successor retains the previous digest, increments the revision and supplies explicit sourced operator authorization for the amendment. Preserve accurate sources for the resulting mode and limit. Request/source references are inspectable claims, not proof of the owner's identity or intent.

Only when that concrete amendment is authorized, apply its retained request:

```text
node "<plugin-root>/scripts/direction-state.mjs" apply --request <request.json>
```

The request must be canonical JSON inside its named run and use the exact expected head. `apply` publishes one exclusive sequence record and does not call a model. Verify the returned record and reread state. A stale head requires a deliberate reload/new request. Exact `already_recorded` replay provides no new dispatch opportunity. `publication_unknown` leaves a potentially published charge/record to reconcile; it is not permission to overwrite, refund or retry a call.

Technical replanning within authorized intent changes the plan, not the intent baseline. Evidence-backed clarification preserves semantic clauses and product decisions. An actual intent change needs a sourced operator-authorized baseline successor. Baseline or policy changes invalidate results bound to old digests.

## Consumption and rollback

Direction-call, content-repair and transport-qualification allowances have separate owners. The default four direction attempts cover initial, endpoint and two repaired endpoints, with no retry headroom. Preparation alone spends no direction slot. A published reservation remains charged across timeout, malformed output, attempted unavailability or missing/interrupted terminal. Reserved-without-terminal attempts are not counted twice. Lowering the limit, disabling or re-enabling the mode refunds nothing; zero or a limit below consumption permits no new reservation. Unknown accounting permits no new dispatch until reconciled from retained evidence.

A completed response is not erased merely because later accounting becomes unknown. Preserve it and its historical terminal; the driver must still resolve the separate allowance judgment and current endpoint eligibility. Do not treat missing output as proof that no call happened.

To roll back an active initialized run:

1. Retain its current ledger, findings/dispositions, source snapshots, full baseline/policy sequence, reservations, results, witnesses and available execution evidence. Record the actual rollback authority and affected run/issue.
2. Reload the current state and retained expected bindings. Construct and apply an explicitly authorized policy successor setting `off`, using the same canonical request procedure above. Do not delete records or edit old policy versions.
3. Verify the published successor and reread state. Preserve every consumed allowance and ordinary verified blocker. Missing evidence remains OUTSTANDING; disabling direction removes only its extra condition.
4. Before later re-enabling, use another sourced successor and recheck current target/baseline/policy/profile and actual qualification. Old results do not become current merely because the same mode is selected again.

Retain-off for future adoption is distinct from rolling back an already initialized run. If an owner selects an earlier release for a genuinely new scope, retain old evidence under its original protocol/profile/release attribution. Never reinterpret newer records through older code or relabel a continuation as a new run to reset its allowance.

Qualification eligibility is not code-byte equality: replacing a pending record with valid qualified metadata can change endpoint eligibility with the same runtime bytes. Validate the actual record and provenance for the current profile after a change. Mock qualification is not real compatibility. A pending/invalid record is enough to withhold promotion and continue ordinary authorized work under an off policy; it does not require an extra qualification experiment to justify retain-off.

## Proposed owner decision wording

The following is proposed text for explicit owner adoption using the attributed evidence. It is not a recorded decision or execution authorization:

> I choose to retain the current direction-off default for [scope/releases], based on the actual #113 NO-GO report, #112 implementation and qualified #111 profile/record attributed above, with their current publication validation retained. I do not authorize default promotion or a new model experiment through this decision. Existing initialized runs retain their current policies unless I separately authorize a specific successor amendment. Findings, prior results and all consumed repair, direction and qualification allowances remain retained. The unachieved behavioral benefit and Epic AC1/AC2/AC4/AC6 remain OUTSTANDING. I choose [continued work / explicit deferral / explicit cancellation or rescoping] for those outcomes; this is not successful delivery of them. Decision source/date: [actual owner source and date].

If the owner selects another option, record that actual option instead of adapting the evidence to this draft. Cancellation/rescoping must be explicit and separate from successful delivery. Silence, an unchanged default and a closed upstream investigation are not owner decisions.

Use recurring verified failures to propose targeted fixtures and reviewed changes with their existing finding/allowance history. Do not append generic global lessons or modify installed skills automatically. Owner approval, configured independent roles, appropriate checks and release rules remain applicable; shipped default or bundled-resource changes require their own authorized implementation and version update.
