# Issue 111 direction audit qualification

Status: **NOT QUALIFIED**. The observed final-protocol invocation was INVALID
because coverage source anchors used arrays where objects are required. The
corrected profile awaits a new actual invocation. The shipped compatibility
record remains pending for corrected profile
`abb20b243641972c135eff402461ed8ff1a85ad332aa5597ba6ffe0e44239cb4`. Deterministic mocked responses
exercise artifact behavior only and do not attest an actual call, semantic
accuracy or author-host confinement.

| Acceptance | Current evidence |
| --- | --- |
| Typed packet/result and response derivation | Deterministic checks; no live judgment claim. |
| Current source-grounded endpoint COMPLETE | Deterministic synthetic representation only; actual final-protocol call OUTSTANDING. |
| Fresh independent observed provider identity | Prior invalid invocation observed the provider alias; corrected-profile invocation OUTSTANDING. |
| Required-mode integration qualification | OUTSTANDING until actual evidence is inspected and the matching record is published. |
| Semantic drift detection | OUTSTANDING; four cases are fixtures, not model evaluation results. |
| Author-host confinement | Not established by the auditor's zero-tool HTTP interface. |

The driver completes the current revision's internal and ordered structural
reviews before a bounded actual invocation. The original unamended experiment
allows two attempted slots, a cumulative 300000ms invocation budget, a 150000ms
process watchdog, 120000ms HTTP bound, 16384 request bytes, 8192 output tokens
and 131072 response bytes. A separately sourced one-attempt amendment may retain
all prior attempts and add one concrete allocation within these unchanged time
and wire limits. No new issue, directory, profile or failure creates an allowance.
The original USD0.10 planning estimate is not a billing guarantee.

Actual history before the corrected-profile invocation contains three driver
attempts and two HTTP requests. The prototype passed in 1900ms with 624 input and
178 output tokens. A later invocation refused noncanonical budget JSON before
HTTP, consuming 37ms and its attempted slot; its usage fields remain unknown.
The final-protocol invocation on revision
`42b15fc96b3ce44cff2597c1aecfe30438f64bb5` consumed 2868ms and returned
HTTP 200, observed alias `deepseek-flash`, finish reason `stop`, no tool calls,
1805 input and 529 output tokens, and zero cached input. Strict extraction
returned `invalid_schema` and retained a null payload. Total charged invocation
time is 4805ms. No failed attempt is refunded or relabeled as qualified.

The invalid invocation used profile
`58ccb60e7ae8f729922dd9736f80f08c4343834f09c682b337dd1eead3289d98` and request
`917a2f343dc1fe54e51c9471faa4f1e36bdc739003d01e3bfeb8e6920abbedeb`.
Its retained response artifact digest is
`ccfa42dbed42f4f58abddca7674133f36d805baa7ab394a74857565ddeb4b91f`, raw wire
response digest is
`ea04c396df894ce92d3e15bfc707807e7f403f4a3d9e9c7f4af4726c4f0653b5`, and result
digest is `eae2dac9bce3e2e69b22cc319c761907c887d7859d674fe76a23d125f0129043`.
An offline diagnostic restored sanitized bindings solely to inspect shape:
converting only singleton source arrays to objects passed validation. That
modified diagnostic is not qualification evidence and replaces no actual bytes.
The repair clarifies container types in the invocation; validation remains strict.

The qualifier's prepare operation creates an isolated synthetic Git/run fixture,
runs its arithmetic checks without credentials, captures actual sources and
writes the final production packet/request and explicit state reservation. It
does not call a provider. `budgetRequest(prepared, physicalCallId)` emits the exact
bound effective-budget request for driver inspection; the driver retains it as canonical
JSON and supplies `probe --prepared <directory> --budget <budget.json>` only after
review and its execution handoff. The same physical call is cross-referenced in
the experiment and synthetic direction state; real issue audit policy stays off.

The qualifier wrapper/setup and fixture bytes are separately bound preparation
evidence. They do not supply an alternative prompt, wire formatter or extractor.
The production profile closes over its explicit local import inventory and works
without qualification fixtures/oracles. Evaluated support needs exactly the
checker and state script entries in addition to automatic library/skill exports;
issue 112 owns those two allowlist additions.

After actual success, the driver finalizes this report with execution revision,
request/response/result/profile hashes, complete retained evidence inventory,
observed usage/identity limits and source/coverage inspection. It then constructs
the qualified record through `buildQualificationRecord`. The report hashes the
actual artifacts and profile, never itself or the qualification record. Its
final bytes supply the record's report digest. Only this report and the
qualification record may change after the call; verify exact runtime carryforward,
rerun network-free endpoint checking and the ordered publication reviews, then
the final native suite.

The record is compatibility evidence adopted through a truthful driver decision,
not cryptographic proof of a provider call. Artifact checks reject inconsistent
response/payload pairs; a fully consistent fabrication remains a level 3
truthfulness boundary. A narrow COMPLETE fixture cannot establish general
semantic reliability.
